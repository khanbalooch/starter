import { ForbiddenNameValidatorDirective } from './forbidden-name.directive';

describe('ForbiddenNameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenNameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
